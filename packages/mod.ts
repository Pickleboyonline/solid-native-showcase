/**
 * Another question to ask is how to deal with external packages.
 * 
 * Most likely will just be a deno package, you import it like everything else, and from there
 * all the native code is linked properly.
 * 
 * The dependencies will just have to all be merged and properly linked.
 */